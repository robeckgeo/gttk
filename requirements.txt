# GeoTIFF ToolKit Dependencies
# GDAL is NOT installable from here: the PyPI `gdal` package builds the Python
# bindings against an existing GDAL C++ library, which pip cannot provide. Install it
# from conda-forge (`conda env create -f environment.yml`) or OSGeo4W first.
#   gdal>=3.11

# JSONPath support for PROJJSON validation
jsonpath-ng>=1.5.0

# Requires Python >= 3.12 (declared in pyproject.toml; not a pip-installable
# requirement, so it is a comment rather than a line pip would choke on)

# Report generation dependencies
lxml>=5.3.1
matplotlib>=3.10.0
mistune>=3.0.0
numpy>=2.2.0
openpyxl>=3.1.5
# ICC profile decoding for the InterColourProfile tag
pillow>=10.0.0
tifffile>=2025.1.10

# Memory detection for adaptive statistics processing
psutil>=5.9.0

# Testing dependencies (optional)
pytest>=7.0.0
pytest-cov>=4.0.0